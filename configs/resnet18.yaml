experiment_name: resnet18

model:
  name: resnet18
  pretrained: true
  image_size: 224

training:
  epochs: 20
  batch_size: 32
  learning_rate: 0.0003
  weight_decay: 0.0001

output:
  dir: outputs/resnet18
